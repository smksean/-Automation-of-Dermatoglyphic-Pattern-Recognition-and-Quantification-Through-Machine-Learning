# Data Inventory and Lineage

This project should be reproducible from the Git repository plus separately
authorized access to the NIST source data and private project secrets. The
repository must not contain the full biometric datasets, private review images,
annotation exports, Streamlit secrets, Supabase service keys, or trained model
payloads.

Use this file as the map when moving the project to another computer.

## Storage Rule

Keep these files in Git:

- source code, notebooks with outputs cleared, tests, aggregate results, figures
  that do not expose subject-linked biometric records, documentation, and model
  checksum manifests

Keep these outside Git:

- raw SD 302 image and EBTS folders
- generated image arrays and review packages under `data/`
- local annotation exports
- `.streamlit/secrets.toml`, `.env`, Supabase keys, and access codes
- checkpoint files such as `*.pt`, `*.pth`, `*.ckpt`, and serialized baselines
  such as `*.joblib`

## Raw Source Data

The current local folder convention is:

| Local path | Purpose | Git status |
|---|---|---|
| `sd302a/images/` | Source fingerprint images used by the broad-pattern workflow. | Ignored |
| `sd302b/images/` | Source fingerprint images used by the broad-pattern workflow. | Ignored |
| `sd302d/images/` | Locally retained SD 302 image source. Not a committed dependency of the current broad-classifier results unless a future workflow explicitly uses it. | Ignored |
| `ebts/baseline/` | SD 302g EBTS/IRR records containing examiner-supplied pattern annotations. | Ignored |
| `ebts/challengers/` | Additional SD 302g EBTS/IRR records and checksums. | Ignored |

If the raw downloads are public official NIST URLs, it is safe to document them
here later. If a link is private, temporary, signed, or account-specific, do not
commit it; store it in the institution-approved private project notes instead.

| Dataset | Approved download/source URL | Notes |
|---|---|---|
| NIST SD 302a | TODO: add official non-secret link | Raw fingerprint images. |
| NIST SD 302b | TODO: add official non-secret link | Raw fingerprint images. |
| NIST SD 302d | TODO: add official non-secret link if still needed | Retained locally; not central to current broad classifier. |
| NIST SD 302g EBTS/IRR | TODO: add official non-secret link | Source of `9.307` pattern annotations. |

## Label Source

The broad labels are not model-generated labels. They are derived from
examiner-supplied ANSI/NIST pattern entries in EBTS field `9.307`, then linked
to the corresponding rolled fingerprint image files.

Current broad mapping:

| EBTS pattern code | Project broad class |
|---|---|
| `AU`, `AU+PA`, `AU+TA` | `arch` |
| `LS` | `left_slant_loop` |
| `RS` | `right_slant_loop` |
| `WU`, `WU+PW`, `WU+CP`, `WU+DL`, `WU+AW` | `whorl` |

Subtype detail is treated more cautiously. Codes such as `AU+PA`, `AU+TA`,
`WU+PW`, `WU+CP`, `WU+DL`, and `WU+AW` provide subtype evidence when present.
Generic `AU` and `WU` identify the main type but do not identify the exact
subtype. Multiple `9.307` entries may be legitimate alternative classifications,
so they must not be collapsed into a single definitive label without review.

## Processed Data Map

All paths in this section are ignored by Git. They should be restored from a
secure copy or regenerated from the raw source data.

| Local path | Used for | Generated by |
|---|---|---|
| `data/processed/irr_pattern_entries.csv` | Flattened pattern entries extracted from EBTS field `9.307`. | `notebooks/01_data_prep.ipynb` |
| `data/processed/irr_image_labels.csv` | Image-level label table after linking EBTS annotations to fingerprint images. | `notebooks/01_data_prep.ipynb` |
| `data/processed/roll_broad_model_split.csv` | Subject-disjoint broad-class modelling table. | `notebooks/02_dataset_review_and_split.ipynb` |
| `data/processed/roll_hog_features.npz` | Feature array for the HOG and linear SVM baseline. | `notebooks/03_model_training_baseline.ipynb` |
| `data/processed/colab_package/` | 160 x 160 CNN package: image array, metadata, label mapping, and README. | `notebooks/04_cnn_preprocessing_for_colab.ipynb` |
| `data/processed/roll_cnn_colab_package.zip` | Portable private zip of the 160 x 160 CNN package for Colab. | `notebooks/04_cnn_preprocessing_for_colab.ipynb` |
| `data/processed/efficientnet_320_package/` | 320 x 320 foreground-cropped, CLAHE-enhanced EfficientNet package. | `scripts/build_roll_320_clahe_package.py` |
| `data/processed/roll_efficientnet_320_package.zip` | Portable private zip of the EfficientNet package for Colab. | `scripts/build_roll_320_clahe_package.py` |
| `data/processed/manual_review_*` | Earlier visual QA and review samples. | Notebook/script review workflow |
| `data/broad_classifier_blind_test/` | Private broad-classifier blind-test material. | `scripts/build_broad_classifier_blind_test.py` |
| `data/processed/client_subtype_relabeling_package/` | Older/broader private relabelling package for arch, whorl, and multi-pattern review. | `scripts/build_client_relabeling_package.py` |
| `data/processed/unlabeled_subtype_review_package_2026-08-04/` | Active private subtype-review package for generic arch and whorl records. | `scripts/build_unlabeled_subtype_review_package.py` |

The EfficientNet package contains the main modelling arrays:

| File | Role |
|---|---|
| `roll_320_clahe_dataset.npz` | Full prepared 320 x 320 array aligned to `roll_320_clahe_metadata.csv`; used to build review packages. |
| `roll_320_clahe_metadata.csv` | Full metadata table, including broad class, subtype evidence, source path, subject, finger, and experiment role. |
| `roll_320_clahe_cv.npz` | Cross-validation images used by the EfficientNet development experiment. |
| `roll_320_clahe_cv_metadata.csv` | Metadata for the cross-validation split. |
| `roll_320_clahe_locked_holdout.npz` | Locked holdout images. Do not use for model selection or subtype-review package construction before final evaluation. |
| `roll_320_clahe_locked_holdout_metadata.csv` | Metadata for the locked holdout. Keep private and unopened until the evaluation plan is frozen. |

## Subtype Review Data

The active subtype-labelling set is:

```text
data/processed/unlabeled_subtype_review_package_2026-08-04/
```

It was built from `data/processed/efficientnet_320_package/` and includes only
non-holdout generic arch and whorl records needing exact subtype review.

Current package audit:

| Measure | Value |
|---|---:|
| Source modelling images | 2,312 |
| Generic arch/whorl subtype gaps found | 808 |
| Review images included | 672 |
| Arch review images | 115 |
| Whorl review images | 557 |
| Subjects represented | 146 |
| Alternative-main-type warnings | 30 |
| Locked-holdout images included | 0 |
| Locked-holdout generic images excluded | 136 |

The review package includes:

- `subtype_labeling_template.csv`
- `images/arch/`
- `images/whorl/`
- `contact_sheets/`
- `START_HERE_INSTRUCTIONS.md`
- `label_guide.txt`
- `broad_label_audit.txt`
- `CHECKSUMS_SHA256.txt`

For cloud review, these images belong in the private Supabase
`fingerprint-review` bucket created by the annotation app migration. Reviewer
answers are written to the database and exported privately as
`exports/subtype_labeling_latest.csv`. That CSV is not committed while it
contains row-level biometric review records.

## Model Artifacts

The repository tracks `models/efficientnet_320_cv/SHA256SUMS.txt` but ignores
the five EfficientNet checkpoint files:

```text
models/efficientnet_320_cv/efficientnet_b0_320_fold_1.pt
models/efficientnet_320_cv/efficientnet_b0_320_fold_2.pt
models/efficientnet_320_cv/efficientnet_b0_320_fold_3.pt
models/efficientnet_320_cv/efficientnet_b0_320_fold_4.pt
models/efficientnet_320_cv/efficientnet_b0_320_fold_5.pt
```

The broad classifier app can download those checkpoint files from a
commit-pinned GitHub media URL and verifies size plus SHA-256 before loading.
Keep a private copy only if offline use is required.

## What To Do Next

Do not push the raw data or review packages to GitHub. For a new laptop:

1. Clone this repository.
2. Recreate the same ignored raw-data folder names from the approved NIST
   downloads or a secure private transfer.
3. Restore or regenerate `data/processed/` from the notebooks and scripts.
4. Keep `.streamlit/secrets.toml` and Supabase service keys private.
5. Treat existing NIST `9.307` links as source annotations for broad labels,
   not as completed expert subtype labels.
6. Continue subtype review through `annotation_app/` and the private Supabase
   backend, or through the local package if cloud storage has not been approved.
7. After expert review, merge only accepted/adjudicated subtype labels into a
   new private derived table. Commit only aggregate statistics or non-sensitive
   code/results unless data-governance approval says otherwise.

The important conceptual split is this: broad classification can already use
the linked NIST codes; subtype classification should wait for the expert-review
outputs because generic `AU` and `WU` do not carry exact subtype labels.
