# URL of the private Python inference service, without a trailing slash.
INFERENCE_API_URL=http://127.0.0.1:8000
# Optional bearer token shared only by Vercel and the private inference service.
INFERENCE_API_TOKEN=
